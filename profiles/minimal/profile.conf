# Minimal sandbox profile — maximally restrictive.
# No /usr, no system dirs, no installs, no shadows.
# The sandbox has only: proc, dev, tmp, home, lib*.

[meta]
description = Minimal sandbox — no /usr, maximally isolated

[user]
comment     = Minimal sandbox account
no-usr      = true
sys-dirs    = false

[sandbox]
hostname    =

[shadow]

[install]

[dotfiles]

[scripts]
